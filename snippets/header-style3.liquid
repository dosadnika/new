{%- if settings.show_multiple_currencies or shop.customer_accounts_enabled or settings.enable_compare -%}
	<div class="header__links header__links--bkg{% if settings.use_sticky %} hide-in-sticky{% endif %}">
		<div class="container{% if settings.header_wide_display %}-fluid{% endif %}">
			<div class="row">
				<div class="col-xs-12">
					<div class="header__links__left f-left">
						{%- render 'currency-switcher', addition_classes: 'header__link--top' -%}
					</div>
					{%- if shop.customer_accounts_enabled or settings.enable_compare or settings.enable_wishlist or settings.enable_language_picker -%}
					<div class="header__links__right f-right">
						{%- render 'language-picker', data_group: 'header__links', show_language_icon: true, addition_classes: 'header__link header__link--top', wrap_class: 'dib' -%}
						{%- if settings.enable_compare -%}
							<a href="javascript:void(0);" class="header__link header__link--top header__link--compare dib{% if enable_rtl %} header__link--reverse{% endif %}" title="{{ 'collections.product_item.compare' | t | escape }}">
								{%- render 'icon-compare' -%}
								{{ 'collections.product_item.compare' | t }}
							</a>
						{%- endif -%}
						{%- if settings.enable_wishlist -%}
							<a href="javascript:void(0);" class="header__link header__link--top header__link--wishlist dib{% if enable_rtl %} header__link--reverse{% endif %}" title="{{ 'general.accessibility.your_wishlist' | t | escape }}">
								{%- render 'icon-wishlist' -%}
								{{ 'general.accessibility.your_wishlist_html' | t }}
							</a>
						{%- endif -%}

						{%- if shop.customer_accounts_enabled -%}
							{%- if customer -%}
								<a href="{{ routes.account_url }}" class="header__link header__link--top dib{% if enable_rtl %} header__link--reverse{% endif %}" title="{{ 'customer.account.title' | t | escape }}">
									{%- render 'icon-user-circle' -%}
									{{ 'customer.account.title' | t }}
								</a>
								<a href="{{ routes.account_logout_url }}" class="header__link header__link--top dib{% if enable_rtl %} header__link--reverse{% endif %}" title="{{ 'layout.customer.log_out' | t | escape }}">
									{%- render 'icon-sign-out' -%}
									{{ 'layout.customer.log_out' | t }}
								</a>
							{%- else -%}	
								<div class="bt-dropdown dib" data-group="header__links">
									<a class="bt-dropdown__trigger header__link header__link--top dib{% if enable_rtl %} header__link--reverse{% endif %}" title="{{ 'customer.account.title' | t | escape }}" href="{{ routes.account_login_url }}">
										{%- render 'icon-sign-in' -%}
										{{ 'customer.login.title' | t }}
									</a>
									<div class="bt-dropdown__content bt-dropdown__content--{% unless enable_rtl %}right{% else %}left{% endunless %}">
										{%- render 'login-form-header', id: 'customer_login_header', suffix_id_field: 'Header', enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
									</div>
								</div>
								<a href="{{ routes.account_register_url }}" class="header__link header__link--top dib{% if enable_rtl %} header__link--reverse{% endif %}" title="{{ 'customer.register.title' | t | escape }}">
									{%- render 'icon-user-circle' -%}
									{{ 'customer.register.title' | t }}
								</a>
							{%- endif -%}
						{%- endif -%}
					</div>
					{%- endif -%}
				</div>
			</div>
		</div>				
	</div>
{%- endif -%}
<div class="container{% if settings.header_wide_display %}-fluid{% endif %}">
	<div class="row header__wrap flex flex-align-ver">
		<div class="col-xs-3 gutter-top gutter-ele-bottom{% if settings.use_sticky %} hide-in-sticky{% endif %}">{% render 'header__logo' %}</div>
		<div class="col-xs-6 gutter-top gutter-ele-bottom text-center{% if settings.use_sticky %} hide-in-sticky{% endif %}">{%- render 'search-form' -%}</div>
		<div class="col-xs-3{% unless settings.header_cart_layout_desktop == 'dropdown' %} gutter-top gutter-ele-bottom{% endunless %} text-right{% if settings.use_sticky %} hide-in-sticky{% endif %}">
			{%- if settings.header_cart_layout_desktop == 'dropdown' -%}
				{%- render 'header-cart' with header_custom_class: 'gutter-top gutter-ele-bottom', cart_dropdown_direction: 'bt-dropdown__content--right', no_flex: true, enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
			{%- else -%}
				{%- render 'header-cart' with enable_rtl: enable_rtl, left_holder: left_holder, right_holder: right_holder -%}
			{%- endif -%}
		</div>
		<div class="col-xs-12 flex flex-align-hoz{% if settings.use_sticky %} use-sticky--desktop{% endif %}" style="position: relative;padding-top:10px;">
			{% section 'navigation' %}
		</div>
	</div>
</div>