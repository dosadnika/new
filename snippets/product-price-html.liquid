<div class="product-price">
  {% if compare_at_price > price %}
    <s class="product-price__price">{%- render 'price', price: compare_at_price -%}</s>
    <span class="product-price__price product-price__sale">
      {%- render 'price', price: price -%}
    </span>
  {% else %}
    <span class="product-price__price">{%- render 'price', price: price -%}</span>
  {% endif %}

  {% unless available %}
    <span class="product-price__sold-out">{{ 'products.product.sold_out' | t }}</span>
  {% endunless %}
  {%- render 'product-price-unit' with variant: variant -%}
</div>