{%- if show_compare_btn and settings.enable_compare -%}
<a {% if showTooltip %} data-toggle="tooltip" data-original-title="{{ 'collections.product_item.add_to_compare' | t | escape }}" {% if tooltip_pos != blank %} data-placement="{{ tooltip_pos }}" {% else %} data-placement="top" {% endif %}{% endif %} title="{{ 'collections.product_item.add_to_compare'|t|escape }}" data-id="{{ product.id }}" href="#" class="button button--cp pull-left flex{% if btn_style_classes %} {{ btn_style_classes }}{% endif %}">{% render 'icon-compare' %}{%- unless hide_compare_text -%}<span class="hidden-xs hidden-sm hidden-md"> {{ 'collections.product_item.compare'|t }}</span>{%- endunless -%}</a>
{%- endif -%}